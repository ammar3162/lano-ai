export default async function handler(req, res) {
  if (req.method !== "POST") {
    return res.status(405).json({ error: "Method not allowed" });
  }

  try {
    const { message } = await req.json();

    // تعليمات المساعد الخاصة بمطعم لانو
    const systemPrompt = `
    أنت مساعد ذكي خاص بمطعم لانو 🍝.
    يجب أن تجيب فقط على الأسئلة المتعلقة بمطعم لانو،
    مثل: المنيو، الأطباق المميزة، أوقات العمل، طرق التواصل، الشيف، الموقع، الفروع، التوصيل، العروض.
    إذا سُئلت عن أي شيء آخر لا يخص مطعم لانو، رد بلطف:
    "أنا فقط مساعد خاص بمطعم لانو، هل ترغب أن أساعدك في معرفة المنيو أو الأصناف المميزة؟"

    ⚙️ المعلومات الأساسية لمطعم لانو:
    - التأسيس: عام 2027 بواسطة الشيف نهى.
    - الفرع: صبيا – منطقة جازان.
    - التوصيل: عبر هنقرستيشن أو المندوبين.
    - أوقات العمل: من 5 مساءً حتى 2 صباحًا (وحتى 3 صباحًا في الويكند).
    - رقم التواصل: 0582003125
    - الموقع: https://maps.app.goo.gl/zvkTWhoxfyRjeDFb94
    - إنستغرام: https://www.instagram.com/lano_pasta/?hl=ar
    - الأصناف المميزة: بيتزا لانو، بيتزا الترافل، بيتزا البيستو، لازانيا، ريزوتو، فوتشيني، كرات البطاطس.
    - فلسفة المطعم: يقدم تجربة إيطالية بروح عصرية ولمسات خيالية.
    `;

    const response = await fetch("https://api.openai.com/v1/chat/completions", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${process.env.OPENAI_API_KEY}`,
      },
      body: JSON.stringify({
        model: "gpt-4o-mini",
        messages: [
          { role: "system", content: systemPrompt },
          { role: "user", content: message },
        ],
      }),
    });

    const data = await response.json();

    if (data.error) {
      return res.status(400).json({ error: data.error.message });
    }

    res.status(200).json({ reply: data.choices[0].message.content });
  } catch (error) {
    res.status(500).json({ error: "حدث خطأ أثناء التواصل مع OpenAI API" });
  }
}
